#pragma once

void exp(const float *_x, float *y, int n);
void fastAtan2(const float *Y, const float *X, float *angle, int len, bool angleInDegrees = true);
void magnitude(const float* x, const float* y, float* mag, int len);
